[project]
name = "claim-verifier"
version = "1.0.0"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "sentence-transformers",
    "numpy",
]

[tool.vercel]
entrypoint = "api.verify:app"
